Tabela_livros:

create table categorias (
    pk_id_categoria int,
    nome varchar,
    descricao text);

create table livros (
    pk_id_livro int,
    titulo varchar,
    isbn varchar,
    fk_id_autor int,
    fk_id_categoria int,
    ano_publicacao int);

